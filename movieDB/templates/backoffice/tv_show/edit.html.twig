{% extends 'base.html.twig' %}

{% block title %}Edit TvShow{% endblock %}

{% block body %}
    <h1>Edit TvShow</h1>

{{ form_start(tv_show) }}
    {{ form_errors(tv_show) }}      
    {# {{ form_errors(tv_show) }} #}
    {{ form_row(tv_show.title) }}
    {{ form_row(tv_show.synopsis) }}
    {{ form_row(tv_show.image) }}
    {{ form_row(tv_show.nbLikes) }} 
    {{ form_row(tv_show.publishedAt) }} 
    {{ form_row(tv_show.createdAt) }} 


    <button type="submit" class="btn btn-primary mt-4">
    {% if 'backoffice_tvshow_add' == app.request.get('_route') %}
    Create
    {% else %}
    Update
    {% endif %}
    </button>

{{ form_end(tv_show) }} 



    <a href="{{ path('backoffice_tv_show_index') }}">back to list</a>

    {# {{ include('backoffice/tv_show/_delete_form.html.twig') }} #}
{% endblock %}
